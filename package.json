{
  "name": "custom-scripts",
  "version": "1.0.0",
    "scripts": {
    "dev": "parcel watch",
    "main": "parcel build --no-source-maps --no-cache",
    "log": "parcel build --log-level verbose",
    "nddorbix": "parcel build src/nddorbix/app-hc-nddorbix.js --dist-dir dist/nddorbix --no-source-maps --no-cache",
    "nddvalidacao": "parcel build src/validacao/app-hc-nddvalidacao.js --dist-dir dist/nddvalidacao --no-source-maps --no-cache"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "parcel": "^2.12.0"
  }
}
